var majorityElement = function(nums) {
    let n = nums.length;
     let majority = n/2;
     let count = {};
   for(let num of nums){
       //hash map
       count[num] = (count[num]||0)+1
       console.log (count);
        if(count[num]>majority){
            console.log(count[num]>majority)
       return num;
   }
 
   }
    
};

console.log(majorityElement([2,2,1,1,1,2,2]))
